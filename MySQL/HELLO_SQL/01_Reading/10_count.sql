-- get the number of rows (all rows) from the table users.
SELECT COUNT(*) FROM hello_sql.users;

-- get the number of not null values from age has in the table users.
SELECT COUNT(age) FROM hello_sql.users;
