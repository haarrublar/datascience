-- get the maximum value from age of all data in the table users.
SELECT MAX(age) FROM hello_sql.users;

-- get the minimum value from age of all data in the table users.
SELECT MIN(age) FROM hello_sql.users;
