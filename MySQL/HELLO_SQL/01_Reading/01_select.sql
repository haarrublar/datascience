-- get the data from the table users. 
SELECT * FROM hello_sql.users;

-- get all names from the table users. 
SELECT name FROM hello_sql.users;

-- get names and user_id from the table users. 
SELECT user_id, name FROM hello_sql.users;